<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 权限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="com.android.launcher.permission.READ_SETTINGS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <application
        android:name=".GongyouTongApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GongyouTong"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true">

        <!-- 百度地图 API Key（请替换为您在 https://lbsyun.baidu.com/ 注册的应用 AK） -->
        <meta-data
            android:name="com.baidu.lbsapi.API_KEY"
            android:value="vzPA8jeqjVqTabbT8nLJap4Bw5Kg2imn" />

        <!-- 百度定位 SDK 必需的服务组件 -->
        <service
            android:name="com.baidu.location.f"
            android:enabled="true"
            android:exported="false"
            android:process=":location" />

        <!-- 主界面 -->
        <activity
            android:name=".ui.main.MainActivity"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 详情页 -->
        <activity
            android:name=".ui.detail.ScheduleDetailActivity"
            android:exported="false"
            android:parentActivityName=".ui.main.MainActivity" />

        <!-- 导航页 -->
        <activity
            android:name=".ui.navigation.NavigationActivity"
            android:exported="false"
            android:parentActivityName=".ui.main.MainActivity" />

        <!-- 百度地图导航页 -->
        <activity
            android:name=".ui.navigation.BaiduNavigationActivity"
            android:exported="false"
            android:parentActivityName=".ui.main.MainActivity"
            android:screenOrientation="portrait" />

        <!-- 个人中心页 -->
        <activity
            android:name=".ui.profile.ProfileActivity"
            android:launchMode="singleTop"
            android:exported="false"
            android:parentActivityName=".ui.main.MainActivity" />

        <!-- 地图设置页 -->
        <activity
            android:name=".ui.settings.SettingsActivity"
            android:launchMode="singleTop"
            android:exported="false"
            android:parentActivityName=".ui.profile.ProfileActivity" />

        <!-- 全部日程页 -->
        <activity
            android:name=".ui.schedule.ScheduleActivity"
            android:launchMode="singleTop"
            android:exported="false"
            android:parentActivityName=".ui.main.MainActivity" />

        <!-- 知识库列表页 -->
        <activity
            android:name=".ui.knowledge.KnowledgeActivity"
            android:launchMode="singleTop"
            android:exported="false"
            android:parentActivityName=".ui.main.MainActivity" />

        <!-- 知识详情页 -->
        <activity
            android:name=".ui.knowledge.KnowledgeDetailActivity"
            android:exported="false"
            android:parentActivityName=".ui.knowledge.KnowledgeActivity" />

        <!-- 工单列表页 -->
        <activity
            android:name=".ui.workorder.WorkOrderActivity"
            android:launchMode="singleTop"
            android:exported="false"
            android:parentActivityName=".ui.main.MainActivity" />

        <!-- 工单详情页 -->
        <activity
            android:name=".ui.workorder.WorkOrderDetailActivity"
            android:exported="false"
            android:parentActivityName=".ui.workorder.WorkOrderActivity" />

        <!-- FileProvider for camera photo sharing -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- 维修界面页 -->
        <activity
            android:name=".ui.repair.RepairActivity"
            android:exported="false"
            android:parentActivityName=".ui.workorder.WorkOrderDetailActivity"
            android:theme="@style/Theme.GongyouTong" />

        <!-- 视频维修界面页 -->
        <activity
            android:name=".ui.repair.VideoRepairActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.MaterialComponents.Light.NoActionBar" />

        <!-- 知识 WebView 阅读页 -->
        <activity
            android:name=".ui.knowledge.KnowledgeWebViewActivity"
            android:exported="false"
            android:parentActivityName=".ui.knowledge.KnowledgeActivity"
            android:theme="@style/Theme.MaterialComponents.Light.NoActionBar" />

        <!-- 客户管理页 -->
        <activity
            android:name=".ui.customer.CustomerActivity"
            android:exported="false"
            android:parentActivityName=".ui.profile.ProfileActivity" />

        <!-- 收入分析页 -->
        <activity
            android:name=".ui.income.IncomeActivity"
            android:exported="false"
            android:parentActivityName=".ui.profile.ProfileActivity" />

    </application>

</manifest>